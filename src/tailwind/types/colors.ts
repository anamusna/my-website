export type Color = {
  primary: string;
  secondary: string;
  text: string;
  background: string;
};

export type Colors = {
  light: {
    primary: string;
    secondary: string;
    text: string;
    background: string;
  };
  dark: {
    primary: string;
    secondary: string;
    text: string;
    background: string;
  };
  royal: {
    primary: string;
    dark: string;
  };
  navy: {
    primary: string;
    hover: string;
  };
  red: {
    primary: string;
  };
  grey: {
    primary: string;
    dividers: string;
    hover: string;
  };
  bg: {
    dark: string;
    light: string;
  };
  black: string;
  white: string;
};

